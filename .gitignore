# compiled binaries (root-level; not the api/ source directory)
/bench
/buildindex
!/api/

# local index files (large, built inside Docker)
*.ivf.bin
*.hnsw.bin

# local dataset copy used for Docker builds (44MB index + json fixtures)
/data/

# test data
/tmp/
rinha-api-bin

# k6 local test artifacts (large files, not committed)
test/test-data.json
test/results.json
