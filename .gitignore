# compiled binaries
/bench
/api
/buildindex

# local index files (large, built inside Docker)
*.ivf.bin
*.hnsw.bin

# local dataset copy used for Docker builds (44MB index + json fixtures)
/data/

# test data
/tmp/
