# local index file (large, built inside Docker)
*.ivf.bin

# local dataset copy used for Docker builds (44MB index + json fixtures)
/data/

# test data
/tmp/
rinha-api-bin

# k6 local test artifacts (large files, not committed)
test/test-data.json
test/results.json
test/results-direct.json
